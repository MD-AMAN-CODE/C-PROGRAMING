//scan function is used to collect input from user

# include<stdio.h>
int main() {
    int a;
       scanf("%d", &a);
       printf("the value of a is %d",a);
    return 0;
}

// multi int..
# include<stdio.h>

int main() {
    int a,b;
    scanf("%d%d",&a,&b);
    printf("%d + %d = %d",a,b,a+b);
    return 0;
}
